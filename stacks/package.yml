---
- name: package stack
  command:
    cmd: >
      docker build target/context
      --file Dockerfile
      --tag {{ stack_image }}:{{ stack_cid }}
    chdir: ../stacks

- name: push stack
  command:
    cmd: docker image push {{ stack_image }}:{{ stack_cid }}
  when: docker_registry != 'localhost'
