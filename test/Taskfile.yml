version: '3'

set: [pipefail]

includes:
  integration:
    taskfile: integration/Taskfile.yml
    dir: integration
  system:
    taskfile: system/Taskfile.yml
    dir: system

tasks:
  run:
    cmds:
      - task: integration:run
      - task: system:run
