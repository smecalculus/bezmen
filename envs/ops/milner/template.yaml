---
solution:
  messaging:
    mapping:
      modes: [spring_mvc, spring_management]
  storage:
    protocol:
      mode: postgres
      postgres:
        url: "jdbc:postgresql://db:5432/{{ database_name }}"
        username: "{{ owner_name }}"
        password: "{{ owner_password }}"
    mapping:
      mode: spring_data
