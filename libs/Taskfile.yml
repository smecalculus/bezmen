version: '3'

set: [pipefail]

vars:
  LIB_REVISION:
    sh: git write-tree --prefix=libs
  LIB_CID: '{{.LIB_REVISION | trunc 7}}'
  LIB_VERSION: 0.1.0-{{.LIB_CID}}
  LIB_BINARIES_STATUS: >
    {{if eq .REGISTRY "localhost" -}}
      task --dir {{.ROOT_DIR}} --status lib:binaries
    {{- else -}}
      true
    {{- end -}}

tasks:
  binaries:
    run: once
    vars:
      SKIP_TESTS: false
    status:
      - mvn -o dependency:get -Dartifact=org.smecalculus.bezmen:libs:{{.LIB_VERSION}}:pom
    cmds:
      - cmd: >
          mvn clean install
          -am -T1C
          --batch-mode
          -DskipTests={{.SKIP_TESTS}}
          -Drevision={{.LIB_VERSION}}
  images:
    run: once
