version: '3'

includes:
  app:
    taskfile: app/Taskfile.yml
    dir: app
  lib:
    taskfile: lib/Taskfile.yml
    dir: lib
  schema:
    taskfile: lib/schema/Taskfile.yml
    dir: lib/schema
  db:
    taskfile: env/db/Taskfile.yml
    dir: env/db

tasks:
  cleanup:
    cmds:
      - docker image prune -f
      - docker volume prune -f
      - docker network prune -f
